{% extends 'base.html.twig' %}

{% block title %}Nos programmes{% endblock %}

{% block body %}
    <div class="container d-flex justify-content-between p-5">
        {% for spectacle in spectacles %}
            <div class="block">
                <h2>{{ spectacle.name }}</h2>
                <p>1ère partie</p>
                <ul>
                    {% for performance in spectacle.performance %}
                        <li>{{ performance.title }}</li><br>
                    {% endfor %}
                </ul>
            </div>
        {% endfor %}
    </div>

{% endblock %}
